import React from "react";
import { LoginForm, RegistrationForm } from "../../components";

const View = (props) => {
  return (
    <div>
      <LoginForm />
      <RegistrationForm />
    </div>
  );
};

export { View };
